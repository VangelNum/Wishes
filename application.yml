spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wisher
    username: wisher
    password: wisher
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect